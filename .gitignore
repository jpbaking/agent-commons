# Local scratch: assembled global-rule files snapshotted from a real install.
# Machine-local and unsynced — the authoritative pointer-block text lives in
# each toolkit's own AGENT-INSTALL.md.
/gists/
